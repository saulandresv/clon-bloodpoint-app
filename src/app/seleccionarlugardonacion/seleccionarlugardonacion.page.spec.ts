import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SeleccionarlugardonacionPage } from './seleccionarlugardonacion.page';

describe('SeleccionarlugardonacionPage', () => {
  let component: SeleccionarlugardonacionPage;
  let fixture: ComponentFixture<SeleccionarlugardonacionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SeleccionarlugardonacionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
